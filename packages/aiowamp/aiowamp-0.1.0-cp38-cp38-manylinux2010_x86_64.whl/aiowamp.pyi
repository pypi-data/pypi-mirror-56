# This file was generated by Nuitka and describes the types of the
# created shared library.

# At this time it lists only the imports made and can be used by the
# tools that bundle libraries, including Nuitka itself. For instance
# standalone mode usage of the created library will need it.

# In the future, this will also contain type information for values
# in the module, so IDEs will use this. Therefore please include it
# when you make software releases of the extension module that it
# describes.

import utils
import aiowamp.args_mixin
import uri
import aiowamp.uri_map
import aiobservable
import contextlib
import warnings
import code
import types
import base64
import abstract
import transport
import bwlist
import hmac
import entry
import asyncio
import entry_point
import websockets.framing
import json
import session
import web_socket
import message
import enum
import dataclasses
import event
import aiowamp.client.invocation
import aiowamp.message
import websockets
import template
import roles
import itertools
import hashlib
import auth
import bisect
import client
import ssl
import typing
import textwrap
import invocation
import call
import abc
import serialization
import serializers
import msgpack
import binascii
import aiowamp
import errors
import urllib.parse
import handler
import id
import logging
import raw_socket
import connect
import args
import inspect

# This is not Python source even if it looks so. Make it clear for
# now. This was decided by PEP 484 designers.
__name__ = ...

