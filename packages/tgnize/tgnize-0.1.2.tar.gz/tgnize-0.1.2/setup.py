#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['tgnize']

package_data = \
{'': ['*'], 'tgnize': ['model/*', 'plotting_scripts/*']}

install_requires = \
['matplotlib', 'typing', 'beautifulsoup4', 'lxml']

extras_require = \
{'dev': ['flit']}

entry_points = \
{'console_scripts': ['tgnize = tgnize.app:main']}

setup(name='tgnize',
      version='0.1.2',
      description='How about another exported Telegram Chat analyzer? 😉',
      author='Anjan Roy',
      author_email='anjanroy@yandex.com',
      url='https://itzmeanjan.github.io/tgnize/',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
