from setuptools import setup

setup(name='gb_distrib',
         version='1.0',
         description='Gaussian and Binomial Distributions',
         packages=['gb_distrib'],
         author='Andrei Ungureanu via Udacity',
         zip_safe=False)