# encoding=utf8
from setuptools import setup

setup(name='RemovePinyinTones',
      version='0.1',
      description='remove the pinyin tone from pinyin string',
      long_description = 'hā -> ha',
      packages=['RemovePinyinTones'],
      zip_safe=False)
