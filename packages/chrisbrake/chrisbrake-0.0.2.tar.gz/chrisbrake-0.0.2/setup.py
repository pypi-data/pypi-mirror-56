#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['chrisbrake', 'chrisbrake.io', 'chrisbrake.io.rec']

package_data = \
{'': ['*']}

install_requires = \
['pynput']

extras_require = \
{'doc': ['sphinx'], 'test': ['flake8', 'pytest', 'pytest-cov']}

entry_points = \
{'console_scripts': ['chrisbrake = chrisbrake:main']}

setup(name='chrisbrake',
      version='0.0.2',
      description='chrisbrake ',
      author='Chris Brake',
      author_email='chris.brake@gmail.com',
      url='https://github.com/chrisbrake/chrisbrake',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
