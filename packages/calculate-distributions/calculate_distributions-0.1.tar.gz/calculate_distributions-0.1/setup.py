from setuptools import setup

setup(name='calculate_distributions',
      version='0.1',
      description='Find Binomaial and Gaussian distributions',
      packages=['calculate_distributions'],
      zip_safe=False)
