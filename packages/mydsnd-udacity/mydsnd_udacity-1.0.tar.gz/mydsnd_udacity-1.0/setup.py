from setuptools import setup

setup(name='mydsnd_udacity',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['mydsnd_udacity'],
      author = ['Ante Kristic'],
      zip_safe=False)
