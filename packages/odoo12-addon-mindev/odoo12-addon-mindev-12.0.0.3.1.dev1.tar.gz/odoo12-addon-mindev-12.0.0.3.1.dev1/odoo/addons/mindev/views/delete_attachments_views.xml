<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="mindev_wizard_delete_attachments_form" model="ir.ui.view">
            <field name="name">mindev.wizard.delete.attachments.form</field>
            <field name="model">mindev.wizard.delete.attachments</field>
            <field name="arch" type="xml">
                <form string="Delete Attachments">
                    <sheet>
                        <group>
                            <p>
                                This wizard will delete all attachemnts that
                                doesn't have corresponding file in filestore.
                                Usually, these 'unlinked' records are the result
                                of a migration.
                            </p>
                        </group>
                        <footer>
                            <button name="action_delete"
                                    type="object"
                                    string="Delete Attachments"
                                    class="oe_highlight"
                            />
                            <button special="cancel" string="Cancel"/>
                        </footer>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="mindev_wizard_delete_attachments_action"
                model="ir.actions.act_window">
            <field name="name">Delete Attachments</field>
            <field name="res_model">mindev.wizard.delete.attachments</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="mindev_wizard_delete_attachments_form"/>
            <field name="target">new</field>
        </record>

        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="mindev_wizard_delete_attachments_menu"
                  name="Delete Attachments"
                  parent="mindev.menu_min_dev"
                  action="mindev_wizard_delete_attachments_action"
                  sequence="50"
        />

    </data>
</odoo>
