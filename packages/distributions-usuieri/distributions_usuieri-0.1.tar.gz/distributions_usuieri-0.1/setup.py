from setuptools import setup

setup(name='distributions_usuieri',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_usuieri'],
      author = 'Erica Usui',
      zip_safe=False)
