#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['attrs ~= 19.3']

entry_points = \
{'flake8.extension': ['implicit-str-concat = '
                      'flake8_implicit_str_concat:Checker']}

setup(name='flake8_implicit_str_concat',
      version='0.0.0.post0',
      description='Forbid implicitly concatenated string literals on one line such as those',
      author='Dylan Turner',
      author_email='58230987+keisheiled@users.noreply.github.com',
      url='https://github.com/keisheiled/flake8-implicit-str-concat',
      py_modules=['flake8_implicit_str_concat'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='~=3.6',
     )
